---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Dr. Anette Larsen - Psykolog i Oslo" description="Velkommen. Her kan du få pusterom. Autorisert psykolog som jobber med voksne, studenter og par i Oslo sentrum.">
  <section class="hero">
    <div class="hero-image">
      <img src="/images/Nordic forest lake.jpg" alt="Stille norsk skog med sollys" class="parallax-bg" />
      <div class="hero-overlay"></div>
    </div>
    <div class="hero-content">
      <div class="container">
        <h1 class="fade-in">Velkommen.<br>Her kan du få pusterom.</h1>
        <p class="hero-text fade-in">
          Å starte i terapi kan kjennes stort. Hos meg kan du ta det i ditt tempo — med rom for undring, uro eller bare en nysgjerrighet på deg selv.
        </p>
        <div class="hero-buttons fade-in">
          <a href="/kontakt" class="btn btn-primary">Bestill time</a>
          <a href="/kontakt" class="btn btn-secondary">Send forespørsel</a>
        </div>
      </div>
    </div>
  </section>

  <section class="intro">
    <div class="container">
      <div class="intro-content">
        <h2 class="fade-in">Et trygt rom for deg</h2>
        <p class="fade-in">
          For meg er terapi først og fremst et møte mellom mennesker. Jeg lytter, stiller spørsmål, og sammen utforsker vi det du ønsker å jobbe med.
        </p>
        <p class="fade-in">
          Mange kommer hit med uro, sorg, eller fastlåste mønstre. Andre kommer fordi noe skurrer — men de vet ikke helt hva. Uansett hvor du er, møter jeg deg der.
        </p>
      </div>
      <div class="intro-image">
        <img src="/images/white-sofa.jpg" alt="Fredelig skogssti" class="fade-in" />
      </div>
    </div>
  </section>

  <section class="meet-anette">
    <div class="container">
      <div class="meet-content">
        <div class="meet-text fade-in">
          <h2>Møt Anette</h2>
          <p class="intro-quote">
            "For meg er terapi først og fremst et møte mellom mennesker. Jeg lytter, stiller spørsmål, og sammen utforsker vi det du ønsker å jobbe med."
          </p>
          <p>
            Jeg er 47 år og har jobbet som psykolog i over 15 år. Jeg driver egen praksis i Oslo sentrum, hvor jeg møter mennesker fra alle samfunnslag og livsfaser.
          </p>
          <a href="/om" class="btn btn-secondary">Les mer om meg</a>
        </div>
        <div class="meet-image fade-in">
          <img src="/images/Psychologist.png" alt="Portrett av Dr. Anette Larsen" />
        </div>
      </div>
    </div>
  </section>

  <section class="themes-preview">
    <div class="container">
      <div class="themes-content fade-in">
        <h2>Mange jeg snakker med bærer på...</h2>
        <p class="themes-intro">
          Disse ordene beskriver noe av det jeg møter i praksis. Kanskje gjenkjenner du noe, eller kanskje har du andre utfordringer — begge deler er helt i orden.
        </p>
        <div class="themes-cloud">
          <span class="theme-tag">Angst</span>
          <span class="theme-tag">Relasjoner</span>
          <span class="theme-tag">Søvn</span>
          <span class="theme-tag">Livskriser</span>
          <span class="theme-tag">Tap</span>
          <span class="theme-tag">Bekymring</span>
          <span class="theme-tag">Utbrenthet</span>
          <span class="theme-tag">Selvbilde</span>
          <span class="theme-tag">Overganger</span>
          <span class="theme-tag">Kommunikasjon</span>
        </div>
        <div class="themes-cta">
          <a href="/terapi#themes" class="btn btn-secondary">Les mer om hvordan vi jobber</a>
        </div>
      </div>
    </div>
  </section>

  <section class="services">
    <div class="container">
      <h2 class="fade-in">Hvem jeg jobber med</h2>
      <div class="services-grid">
        <div class="service-card fade-in">
          <h3>Voksne</h3>
          <p>Individuell terapi for voksne som ønsker å forstå seg selv bedre eller arbeide gjennom utfordringer.</p>
        </div>
        <div class="service-card fade-in">
          <h3>Studenter</h3>
          <p>Spesialisert oppfølging for studenter som møter studierelaterte utfordringer eller livsfaseoverganger.</p>
        </div>
        <div class="service-card fade-in">
          <h3>Par</h3>
          <p>Parterapi for dem som ønsker å styrke relasjonen eller jobbe gjennom konflikter og kommunikasjonsmønstre.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="approach">
    <div class="container">
      <div class="approach-content">
        <div class="approach-text">
          <h2 class="fade-in">Min tilnærming</h2>
          <p class="fade-in">
            Jeg jobber blant annet med kognitiv terapi og mindfulness — men metoden tilpasses deg, og vi finner veien sammen.
          </p>
          <p class="fade-in">
            Samtaler foregår på norsk eller engelsk, etter din preferanse. Jeg har erfaring med angst, relasjoner, søvn, livskriser, tap, bekymring og utbrenthet.
          </p>
          <a href="/terapi" class="btn btn-secondary fade-in">Les mer om terapi</a>
        </div>
      </div>
    </div>
  </section>

  <section class="booking-invitation">
    <div class="container">
      <div class="booking-content fade-in">
        <h2>Klar for en samtale?</h2>
        <p>
          Du trenger ikke ha alt klart før vi møtes. Kom som du er, med det du har på hjertet akkurat nå.
        </p>
        <div class="booking-options">
          <a href="/kontakt" class="btn btn-primary">Bestill time</a>
          <a href="/kontakt" class="btn btn-secondary">Send forespørsel</a>
        </div>
      </div>
    </div>
  </section>

  <section class="practical">
    <div class="container">
      <h2 class="fade-in">Praktisk informasjon</h2>
      <div class="practical-grid">
        <div class="practical-card fade-in">
          <h3>Priser</h3>
          <p>50 min sesjon: <strong>1.200 NOK</strong><br>
          Helger: <strong>1.650 NOK</strong></p>
        </div>
        <div class="practical-card fade-in">
          <h3>Format</h3>
          <p>På kontoret i Oslo sentrum<br>
          eller digitalt via sikker videolink</p>
        </div>
        <div class="practical-card fade-in">
          <h3>Avbestilling</h3>
          <p>Senest 24 timer før avtalt tid<br>
          for å unngå full pris</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    position: relative;
    height: 100vh;
    min-height: 600px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: flex-start;
  }

  .hero-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
  }

  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(0, 66, 37, 0.4) 0%, rgba(146, 173, 67, 0.2) 100%);
    z-index: -1;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    width: 100%;
    padding-top: 80px;
  }

  .hero h1 {
    color: var(--white);
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    margin-bottom: 2rem;
  }

  .hero-text {
    font-size: 1.3rem;
    line-height: 1.6;
    color: var(--white);
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
    max-width: 600px;
    margin-bottom: 3rem;
  }

  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  .intro {
    padding: 6rem 0;
    background: var(--pale-blue);
  }

  .intro .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .intro-content h2 {
    color: var(--deep-spruce);
    margin-bottom: 2rem;
  }

  .intro-image img {
    width: 100%;
    height: 400px;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .meet-anette {
    padding: 6rem 0;
    background: linear-gradient(135deg, rgba(146, 173, 67, 0.05) 0%, rgba(227, 236, 241, 0.3) 100%);
  }

  .meet-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .meet-text h2 {
    margin-bottom: 2rem;
  }

  .intro-quote {
    font-size: 1.3rem;
    font-style: italic;
    color: var(--deep-spruce);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .meet-image img {
    width: 100%;
    height: 400px;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .themes-preview {
    padding: 6rem 0;
    background: var(--white);
  }

  .themes-content {
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
  }

  .themes-content h2 {
    margin-bottom: 2rem;
  }

  .themes-intro {
    font-size: 1.1rem;
    color: var(--woodland-gray);
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  .themes-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 3rem;
  }

  .theme-tag {
    display: inline-block;
    padding: 0.8rem 1.5rem;
    background: var(--pale-blue);
    color: var(--trunk-brown);
    border-radius: 50px;
    font-size: 1rem;
    font-weight: 500;
    transition: all 0.3s ease;
    cursor: default;
  }

  .theme-tag:hover {
    background: var(--moss-green);
    color: var(--white);
    transform: translateY(-2px);
  }

  .themes-cta {
    margin-top: 2rem;
  }

  .services {
    padding: 6rem 0;
    background: var(--pale-blue);
  }

  .services h2 {
    text-align: center;
    margin-bottom: 4rem;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .service-card {
    background: var(--pale-blue);
    padding: 3rem 2rem;
    border-radius: 20px;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
  }

  .service-card h3 {
    color: var(--deep-spruce);
    margin-bottom: 1.5rem;
  }

  .approach {
    padding: 6rem 0;
    background: linear-gradient(135deg, rgba(146, 173, 67, 0.05) 0%, rgba(227, 236, 241, 0.5) 100%);
  }

  .approach-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .approach h2 {
    margin-bottom: 2rem;
  }

  .approach p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
  }

  .booking-invitation {
    padding: 6rem 0;
    background: linear-gradient(135deg, var(--moss-green) 0%, var(--deep-spruce) 100%);
    color: var(--white);
  }

  .booking-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .booking-content h2 {
    color: var(--white);
    margin-bottom: 2rem;
  }

  .booking-content p {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  .booking-options {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .booking-options .btn-primary {
    background: var(--white);
    color: var(--deep-spruce);
  }

  .booking-options .btn-primary:hover {
    background: var(--pale-blue);
    transform: translateY(-2px);
  }

  .booking-options .btn-secondary {
    border-color: rgba(255, 255, 255, 0.5);
    color: var(--white);
  }

  .booking-options .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: var(--white);
  }

  .practical {
    padding: 6rem 0;
  }

  .practical h2 {
    text-align: center;
    margin-bottom: 4rem;
  }

  .practical-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .practical-card {
    background: var(--white);
    border: 2px solid var(--moss-green);
    padding: 2rem;
    border-radius: 15px;
    text-align: center;
    transition: all 0.3s ease;
  }

  .practical-card:hover {
    background: var(--pale-blue);
    transform: translateY(-3px);
  }

  .practical-card h3 {
    color: var(--deep-spruce);
    margin-bottom: 1rem;
  }

  .practical-card strong {
    color: var(--moss-green);
  }

  @media (max-width: 768px) {
    .hero {
      height: 80vh;
      min-height: 500px;
    }

    .hero h1 {
      font-size: 2.5rem;
    }

    .hero-text {
      font-size: 1.1rem;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: flex-start;
      align-items: center;
    }

    .hero-buttons .btn {
      width: 100%;
      max-width: 280px;
    }

    .meet-content {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .meet-image {
      order: -1;
    }

    .meet-image img {
      height: 300px;
    }

    .themes-cloud {
      justify-content: flex-start;
    }

    .theme-tag {
      font-size: 0.9rem;
      padding: 0.6rem 1.2rem;
    }

    .intro .container {
      grid-template-columns: 1fr;
      gap: 2rem;
      text-align: center;
    }

    .intro-image {
      order: -1;
    }

    .intro-image img {
      height: 250px;
    }

    .services, .approach, .practical, .meet-anette, .themes-preview, .booking-invitation {
      padding: 4rem 0;
    }

    .services-grid, .practical-grid {
      grid-template-columns: 1fr;
    }

    .booking-options {
      flex-direction: column;
      align-items: center;
    }

    .booking-options .btn {
      width: 100%;
      max-width: 280px;
    }
  }

  @media (max-width: 480px) {
    .hero {
      height: 70vh;
      justify-content: center;
      text-align: center;
    }

    .hero h1 {
      font-size: 2rem;
    }

    .hero-text {
      font-size: 1rem;
    }

    .service-card, .practical-card {
      padding: 2rem 1.5rem;
    }

    .intro-quote {
      font-size: 1.1rem;
    }
  }

  /* Parallax effect */
  @media (prefers-reduced-motion: no-preference) {
    .parallax-bg {
      will-change: transform;
    }
  }
</style>

<script>
  // Parallax effect for hero image
  window.addEventListener('scroll', () => {
    if (window.matchMedia('(prefers-reduced-motion: no-preference)').matches) {
      const scrolled = window.pageYOffset;
      const parallaxBg = document.querySelector('.parallax-bg');
      if (parallaxBg) {
        const speed = scrolled * 0.5;
        parallaxBg.style.transform = `translateY(${speed}px)`;
      }
    }
  });
</script>